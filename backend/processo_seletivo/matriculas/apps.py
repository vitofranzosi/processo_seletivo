from django.apps import AppConfig


class MatriculasConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "processo_seletivo.matriculas"
    verbose_name = "Exportação de matrículas"
